#!/bin/bash
# claude-meter macOS launcher (double-click to run `claude-meter start`).
#
# アイコンを変更したい場合は、Finder で対象の .command ファイルを選択し、
# Cmd+I (情報を見る) でタイトルバーのアイコンに画像をドラッグ&ドロップして
# ください。
#
cd "__REPO_ROOT__" || exit 1
claude-meter start
